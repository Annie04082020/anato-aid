# Core Python environment
torch==2.0.1+cu118         # 確保和 CUDA 相容 (你可以改成你的版本)
torchvision==0.15.2+cu118
torchaudio==2.0.2+cu118
# 上面這三個建議從 https://pytorch.org/get-started/locally/ 抓符合你 CUDA 的版本安裝

# 安裝 MIM (MM 系列用)
openmim==0.3.9

# MM 系列基礎套件
mmengine==0.10.3
mmcv==2.0.1

# MMDetection (v3.2.0) 與 MMPose (v1.3.0)，用 mim 安裝
# 這裡先不直接寫在這個 requirements.txt 中，而是你在建立完環境後再跑：
# mim install mmdet==3.2.0
# mim install mmpose==1.3.0

# 其他基礎依賴
numpy
matplotlib
pycocotools
scipy
opencv-python
Pillow

# 若你要讀寫 yaml, json, tqdm 類
PyYAML
tqdm

# 如果你會跑 inference
onnxruntime
